kind = "web"
previewPath = "/"
title = "Room Preview App"
version = "1.0.0"

[[services]]
localPort = 3000
name = "web"
paths = ["/"]

[services.development]
run = "cd room-preview && PORT=3000 npm run dev"
